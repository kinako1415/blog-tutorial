const CreatePost = () => {
  const loginInWithGoogle = () => {
    //googleでログイン
  };
  return (
    <div>
      <p>ログインして始める</p>
      <button onClick={loginInWithGoogle}>googleでログイン</button>
    </div>
  );
};

export default CreatePost;
